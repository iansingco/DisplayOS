{
  "name": "displayos",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["server", "client", "admin", "bridge"],
  "scripts": {
    "start":  "node launcher.js",
    "dev":    "node launcher.js",
    "bridge": "npm run dev --workspace=bridge",
    "server": "npm run dev --workspace=server",
    "client": "npm run dev --workspace=client",
    "admin":  "npm run dev --workspace=admin"
  }
}
