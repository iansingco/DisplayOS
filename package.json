{
  "name": "displayos",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["server", "client", "admin"],
  "scripts": {
    "dev": "concurrently -n server,client,admin -c cyan,blue,magenta \"npm run dev --workspace=server\" \"npm run dev --workspace=client\" \"npm run dev --workspace=admin\"",
    "install:all": "npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
